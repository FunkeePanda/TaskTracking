import { View, Text } from 'react-native'

export default function TodayScreen() {
	return (
		<View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
			<Text style={{ fontSize: 18 }}>Today Screen</Text>
		</View>
	)
}

